{
  "name": "portafolio-edwin-standalone",
  "version": "1.0.0",
  "description": "Portafolio profesional de Edwin Chavarro Trujillo",
  "scripts": {
    "minify:css": "cssnano style.css style.min.css",
    "minify:js": "terser scripts.js -o scripts.min.js -c -m",
    "build": "npm run minify:css && npm run minify:js",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "cssnano": "^6.0.0",
    "terser": "^5.0.0"
  }
}